# 原有菜品管理页面备份

此处为合并 React 前端之前、纯 HTML/CSS/JS 版本的备份。

- **add-dish.html**：新建/编辑菜品页面。
- 若你本地仍有 **index.html**、**css/**、**js/** 的备份，可一并放入此目录以便参考；业务逻辑与数据模型可据此迁移到 React。

当前主入口为根目录的 **index.html**（Vite + React），运行 `npm run dev` 启动。
